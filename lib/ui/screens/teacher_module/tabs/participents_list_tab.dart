import 'package:flutter/material.dart';

class ParticipantsListTab extends StatelessWidget {
  const ParticipantsListTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Participants List"),);
  }
}
